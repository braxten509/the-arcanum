id = "s03"
codename = "CHAPTER III // MAKE THE PADDLES MOVE"
short = "Make the Paddles Move"
title = "Make the Paddles Move"
build = "turn the window into a paced event loop that polls and interprets keyboard state, handles simultaneous paddle controls, clamps positions, advances frames, and keeps quit responsive through explicit integer state, control flow, procedures, and loop checks."
brief = "Turn the learner-owned court into a responsive two-player loop: drain quit events, sample simultaneous keyboard controls, clamp both paddles, pace each frame to a deadline, and prove the same procedures with deterministic input and clock controls."

[proof]
mode = "run"
mechanisms = ["sdl-event-poll-type-and-quit-dispatch", "sdl-keyboard-snapshot-and-scancode-indexing", "integer-paddle-delta-and-boundary-clamp", "sdl-ticks-deadline-and-delay-pacing", "abi-safe-poll-update-render-loop", "deterministic-input-clock-and-frame-limit-controls", "responsive-loop-check-gdb-and-live-proof"]
expectedFiles = ["src/pong.asm", "Makefile"]
runArgs = ["--arcanum-loop-check"]
expect = "PADDLES_OK"
