version = 4
trial = "role-specific-edit-v1; reviewer=authored-repair-replay-v3+real-hollowcrawl-blind-audit-v1"
runDate = "2026-07-14"
description = "Primary Reviewer approval is binary: pass authored repair/replay twice and find every blocker in the frozen real HollowCrawl false-pass artifact. Verification-only recovery is separate. Production denies override compact passes. Two operator-graded exceptions exist: Kimi K2.6 is approved as an economy primary ONLY behind the harness escalation chain (Sol/Fable in [autonomy].8), and Fable 5 is operator-designated at high effort without a completed v3 trial."

[reviewerReliability]
suite = "authored-repair-replay-v3 + real-hollowcrawl-blind-audit-v1"
requiredOutcome = "PASS"
requiredConsecutivePasses = 2
candidateCount = 3
cleanControls = 1
mandatoryCommands = 17
approved = ["gpt-5.6-sol"]
approvedWithEscalation = ["opencode-go/kimi-k2.6"]
operatorApproved = ["claude-fable-5"]
verificationOnly = ["gpt-5.6-terra"]
firstRun = "20260714-182828-3031312"
confirmationRun = "20260714-184149-3146789"
realAuditRun = "20260714-192546-3512184"
realAuditRequiredRootCauses = 3
productionDeniedDespiteCompactPass = ["Gemini 3.5 Flash (High)"]

[realReviewerAudit]
case = "hollowcrawl-phase8-false-pass"
scope = "ordinary cold start and acceptance integrity; inspect behind the first crash"
requiredRootCauses = ["float tile viewport bounds", "missing draw_inventory import", "constant acceptance PASS"]
passed = ["gpt-5.6-sol"]
failed = ["opencode-go/kimi-k2.6", "opencode-go/glm-5.2", "gpt-5.6-terra", "Gemini 3.5 Flash (High)", "Gemini 3.1 Pro (High)"]
themeExcluded = true
report = ".tome-build/hollowcrawl-review-trials/20260714-192546-3512184/results.json"

[models."opencode-go/deepseek-v4-flash"]
pass = ["drafter"]
fail = []
scores = { drafter = "6/6" }
elapsedSeconds = 17.85

[models."opencode-go/minimax-m3"]
pass = ["writer", "sections"]
fail = ["reviewer"]
scores = { writer = "7/7", sections = "8/8", reviewer = "6/7" }
elapsedSeconds = 89.23
note = "Reviewer emitted five findings for three root causes, including a false environment-propagation blocker."

[models."opencode-go/kimi-k2.6"]
pass = ["reviewer"]
fail = []
scores = { reviewer = "conditional: PASS v3 (2/2); real audit 2/3; primary only behind the Sol/Fable escalation chain" }
reviewerPasses = 2
reviewerRequired = 2
reviewerRunsSeconds = [117.74, 157.46]
reviewerRealAuditRootCauses = 2
note = "Passed compact v3 twice but stopped after HollowCrawl's visible crash and counterfeit receipt, missing the guaranteed next-frame draw_inventory NameError. Operator re-graded it as the economy primary because [autonomy].8 guarantees Sol/Fable escalation and the deterministic launch/acceptance gates now surface latent blockers it may stop short of."

[models."gpt-5.6-terra"]
pass = ["writer", "sections", "review_verifier"]
fail = ["reviewer"]
scores = { writer = "7/7", sections = "8/8", review_verifier = "PASS v3 (2/2); verification-only", reviewer = "FAIL real audit: 2/3 root causes" }
reviewerPasses = 2
reviewerRequired = 2
reviewerRunsSeconds = [92.46, 118.18]
reviewerRealAuditRootCauses = 2
note = "Passed compact v3 twice and remains the distinct post-repair verifier/general backup, but missed HollowCrawl's latent draw_inventory NameError, so it is not selectable as the primary Reviewer."

[models."opencode-go/qwen3.7-plus"]
pass = ["writer"]
fail = []
scores = { writer = "7/7" }
elapsedSeconds = 56.12

[models."opencode-go/deepseek-v4-pro"]
pass = []
fail = ["sections"]
scores = { sections = "7/8" }
elapsedSeconds = 36.27
note = "Acceptance toggled literal booleans instead of exercising the repaired game functions."

[models."opencode-go/glm-5.2"]
pass = []
fail = ["reviewer"]
scores = { reviewer = "FAIL real audit: 2/3 root causes after PASS v3 (2/2)" }
reviewerPasses = 2
reviewerRequired = 2
reviewerRunsSeconds = [244.51, 123.52]
reviewerRealAuditRootCauses = 2
note = "Passed compact v3 twice but missed HollowCrawl's latent draw_inventory NameError. Primary Reviewer disabled."

[models."gpt-5.6-luna"]
pass = ["drafter", "writer"]
fail = []
scores = { drafter = "6/6", writer = "7/7" }
elapsedSeconds = 105.10

[models."claude-sonnet-5"]
pass = ["writer", "sections"]
fail = ["reviewer"]
scores = { writer = "7/7", sections = "8/8", reviewer = "FAIL v3: budget exhausted before Ashen repair" }
elapsedSeconds = 115.73
budgetExceeded = true
note = "Earlier Writer and Sections artifacts passed. The separate Reviewer v3 run exhausted its 0.40 USD cap before repairing Ashen Ledger and checking the clean control."

[models."gpt-5.6-sol"]
pass = ["reviewer"]
fail = []
scores = { reviewer = "PASS v3 (2/2) + real audit (3/3)" }
reviewerPasses = 2
reviewerRequired = 2
reviewerRunsSeconds = [139.48, 127.07]
reviewerRealAuditRootCauses = 3
note = "Twice closed every compact repair/replay gate and was the only tested model to inspect behind HollowCrawl's first crash and find all three real blockers."

[models."claude-opus-4-8"]
pass = ["writer"]
fail = ["reviewer"]
scores = { writer = "7/7", reviewer = "FAIL v3: budget exhausted with 11 critical gates open" }
elapsedSeconds = 55.40
note = "Writer remains passed. Reviewer v3 exhausted its 0.40 USD cap before completing the first candidate and never reached repair closure."

[models."opencode-go/qwen3.7-max"]
pass = []
fail = ["reviewer"]
scores = { reviewer = "FAIL v3: timed out with 8 critical gates open" }
elapsedSeconds = 300.04
note = "Timed out at the five-minute cap without a complete v3 report or repair closure."

[models."Gemini 3.5 Flash (High)"]
pass = ["drafter", "writer", "sections"]
fail = ["reviewer"]
scores = { drafter = "6/6", writer = "7/7", sections = "8/8", reviewer = "FAIL real audit: 2/3; compact v3 1/2; production denied" }
reviewerPasses = 1
reviewerRequired = 2
elapsedSeconds = 108.07
reviewerRealAuditRootCauses = 2
note = "Caught the visible real crash and constant receipt on retry but missed the latent draw_inventory NameError; the original full-tome false PASS remains decisive."

[models."Gemini 3.1 Pro (High)"]
pass = ["drafter", "writer", "sections"]
fail = ["reviewer"]
scores = { drafter = "6/6", writer = "7/7", sections = "8/8", reviewer = "FAIL v3: returned with 12 critical gates open" }
elapsedSeconds = 249.21
reviewerRealAuditRootCauses = 2
note = "Returned normally but did not complete either v3 repair target; its real audit also stopped after two of three root causes."

[productionDeny."Gemini 3.5 Flash (High)"]
reviewer = "A compact explicit repair pass cannot override its real full-tome Phase 8 false PASS on a project whose ordinary entrypoint crashed and whose acceptance proof was constant."

[productionDeny."opencode-go/glm-5.2"]
reviewer = "The blind real HollowCrawl audit stopped after the first crash and missed the guaranteed next launch blocker."

[productionDeny."gpt-5.6-terra"]
reviewer = "Primary review is denied after missing the latent HollowCrawl blocker; Terra remains verification-only because it passed repair/replay twice."
