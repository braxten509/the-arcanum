/* ARCANUM game engine — the desk the tomes rest on. The engine itself lives in app/.
   This stays a classic script so index.html's <script src="/app.js"> loads unchanged;
   the dynamic import pulls in the module tree. */
import("/app/main.js");
