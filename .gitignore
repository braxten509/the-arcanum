# Python
__pycache__/
*.pyc

# Runtime ephemera + per-user progress (regenerated on launch)
.cache/
web/.forge-trace/
tomes/*/save/

# build_tome.py harness state (plan + verdict files)
.tome-build/

# Editor/local
.claude/settings.local.json
*.bak

# Per-user secrets + config (Pushover token, AI API keys) and runtime logs — never commit
global-configs/settings.toml
server.log
