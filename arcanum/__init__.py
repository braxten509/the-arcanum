"""ARCANUM application, evidence, authoring, runtime, and HTTP packages.

``server.py`` is the composition shell; browser APIs are registered explicitly
under :mod:`arcanum.http`, and static-file delivery is a separate adapter.
"""
