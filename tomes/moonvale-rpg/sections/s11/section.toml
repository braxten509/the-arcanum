id = "s11"
codename = "CHAPTER XI // READ THE ENGINE'S TRACKS"
short = "Read the Engine's Tracks"
title = "Read the Engine's Tracks"
build = "use tracebacks, logging, debug overlays, pytest, profiler evidence, collision broad-phase improvements, and a release checklist to diagnose and polish the integrated game."
brief = "Before sealing the atlas, Rowan asks for evidence instead of guesswork. You will put logs, collision outlines, and animation labels behind one opt-in flag; reduce wall candidates with a spatial index; measure the query boundary; and turn media, transient-file, launch, and test expectations into a reusable release audit."

[proof]
mode = "run"
expectedFiles = ["main.py", "src/moonvale/diagnostics.py", "src/moonvale/spatial.py", "src/moonvale/profiling.py", "src/moonvale/release.py", "tests/test_diagnostics.py", "tests/test_spatial.py"]
runArgs = ["--arcanum-proof", "s11"]
expect = "S11_OK debug=False candidates=1 calls=3 credits=True release=ready"
