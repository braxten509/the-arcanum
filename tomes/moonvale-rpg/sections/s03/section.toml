id = "s03"
codename = "CHAPTER III // GIVE THE HERO FOOTSTEPS"
short = "Give the Hero Footsteps"
title = "Give the Hero Footsteps"
build = "model the player with classes and vectors, read keyboard intent, normalize movement, resolve rectangle collision, and prove walking cannot cross a wall."
brief = "The wheel turns, but Moonvale still has no traveler. Rowan asks you to give one <code>Player</code> ownership of position, direction, rectangle, and stats; convert the established <code>InputSnapshot</code> into frame-rate-independent motion; and stop that motion against a wall with repeatable evidence."

[proof]
mode = "run"
expectedFiles = ["main.py", "src/moonvale/app.py", "src/moonvale/input.py", "src/moonvale/settings.py", "src/moonvale/entities.py", "src/moonvale/collision.py"]
runArgs = ["--arcanum-proof", "s03"]
expect = "S03_OK position=64,32 blocked=True hp=10"
