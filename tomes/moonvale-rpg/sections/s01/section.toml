id = "s01"
codename = "CHAPTER I // RAISE THE WORKSHOP"
short = "Raise the Workshop"
title = "Raise the Workshop"
build = "prepare the external Python workspace and learn each required syntax, collection, function, import, class, string, path, and error-reading fundamental through tiny runnable probes before assembling the Moonvale package."
brief = "Rowan lays out the real mapmaker's bench before asking for a game. You will create an isolated Python workspace, learn the language pieces that later systems depend on, read failures as evidence, and leave behind a runnable <code>MoonvaleRPG</code> package rather than a pile of disconnected probes."

[proof]
mode = "run"
expectedFiles = ["main.py", "pyproject.toml", "requirements.txt", "src/moonvale/__init__.py", "src/moonvale/__main__.py", "src/moonvale/settings.py", "src/moonvale/model.py", "src/moonvale/app.py"]
runArgs = ["--arcanum-proof", "s01"]
expect = "S01_OK project=MoonvaleRPG modules=3"
