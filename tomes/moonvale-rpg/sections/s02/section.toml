id = "s02"
codename = "CHAPTER II // TURN THE GAME WHEEL"
short = "Turn the Game Wheel"
title = "Turn the Game Wheel"
build = "open a PyGame window and build one controlled event-update-render loop with injected input, time, seed, frame limit, clean quit, and the first headless `boot-title` proof."
brief = "A game is not a chain of screens; it is one wheel that repeatedly gathers events, advances state, and paints the newest frame. You will build that wheel with injectable edges, so the visible PyGame window and the headless proof exercise the same <code>Game</code> lifecycle."

[proof]
mode = "run"
expectedFiles = ["main.py", "src/moonvale/app.py", "src/moonvale/game.py", "src/moonvale/input.py", "src/moonvale/scenes.py", "src/moonvale/testing.py"]
runArgs = ["--arcanum-proof", "s02"]
expect = "S02_OK scene=title frames=3 quit=clean"
