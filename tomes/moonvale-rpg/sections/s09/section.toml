id = "s09"
codename = "CHAPTER IX // BOTTLE A MOMENT"
short = "Bottle a Moment"
title = "Bottle a Moment"
build = "serialize versioned player/world/quest/inventory state to JSON, write saves atomically, reload them, validate bad data, and recover clearly from missing or corrupt files."
brief = "A save is untrusted external data, not a memory dump. You will define one versioned JSON shape, validate every public key, replace files atomically, quarantine corrupt input, and restore the existing Player, Inventory, and QuestLog owners without renaming their state."

[proof]
mode = "run"
expectedFiles = ["main.py", "src/moonvale/savegame.py", "src/moonvale/app.py", "tests/test_savegame.py"]
runArgs = ["--arcanum-proof", "s09"]
expect = "S09_OK version=1 quest=completed herbs=3 hp=8 recovered=True"
