# Section s01 — Phase 2 skeleton. Phase 3 expands the lesson and working.
# One lesson with one of each core exercise type is shown; author 3-5 lessons per
# section and 4-6 mixed exercises per lesson (see § sections/<sid>.toml).
id = "s01"
codename = "FORGE OP I // FIRST SPARKS"
short = "First Sparks"
title = "First Sparks"
build = "Assemble, link, run, exit cleanly, and print one fixed string through sys_write."
brief = "The forge is lit only when real metal runs. Strike the smallest NASM program that enters at <code>_start</code>, speaks through <code>sys_write</code>, and leaves through <code>sys_exit</code>."
