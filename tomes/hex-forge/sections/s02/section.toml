# Section s02 — Phase 2 skeleton. Phase 3 expands the lesson and working.
id = "s02"
codename = "FORGE OP II // ORE TO INGOT"
short = "Ore to Ingot"
title = "Ore to Ingot"
build = "Parse decimal bytes, perform integer arithmetic, and print numbers without libc."
brief = "Numbers are the alloy. This op draws raw digit bytes into registers, works them with <code>add</code>, <code>sub</code>, <code>imul</code>, and <code>idiv</code>, then hammers them back into text."
