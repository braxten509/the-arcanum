# Section s07 — Phase 2 skeleton. Phase 3 expands the lesson and working.
id = "s07"
codename = "FORGE OP VII // PRYING THE VAULT"
short = "Prying the Vault"
title = "Prying the Vault"
build = "Inspect argc and argv at _start, find syscall numbers, open a named file, and fall back to stdin."
brief = "Before you strike, find the gate. This op teaches the reconnaissance: stack layout, argv pointers, <code>openat</code>, <code>read</code>, <code>close</code>, and the table that names them."
