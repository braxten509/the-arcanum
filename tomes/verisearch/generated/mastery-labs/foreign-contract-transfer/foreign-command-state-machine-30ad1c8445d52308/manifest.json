{
  "aidPolicy": "cold",
  "axes": {
    "domain": "observatory-instrument",
    "failure-mode": "1",
    "input-shape": ":",
    "output-order": "ordinal ascending"
  },
  "blueprintId": "foreign-command-state-machine",
  "brief": "Build an unrelated observatory-instrument state register, not a search or citation feature. The executable reads commands only from standard input. The literal separator is ':', failure observation mode is 1 (1 means count malformed lines; 0 means report zero), and the required key ordering is ordinal ascending. These are fixed candidate constants rather than runtime choices. The public requirements define the complete grammar, transition, bounds, newline rules, and six-line output contract; no command-line arguments or external files are used.",
  "capabilityIds": [
    "language-types-nullability",
    "language-control-flow",
    "language-collections-generics",
    "language-methods-scope",
    "language-classes-records"
  ],
  "cognitiveTasks": [
    "trace",
    "integrate",
    "explain"
  ],
  "contentHash": "024336b19e14324b0500ad5b7259c12d7a7e6b77324e286542be424a15d56ac5",
  "dependencies": [],
  "difficulty": "novel C# transfer",
  "estimatedMinutes": 60,
  "familyId": "foreign-contract-transfer",
  "publicExamples": [
    "For separator ':', SET:alpha:+02 and SET:gamma_2:-0 are lexically valid; SET:Alpha:2, SET:alpha: 2, and set:alpha:2 are malformed.",
    "Every distinct accepted key must appear in the ordered snapshot, so a fixed alpha/beta scalar implementation is insufficient.",
    "The rationale is part of the submission, but the public prompt intentionally does not supply its trace or mutation evidence."
  ],
  "rationalePrompt": "Trace one representative input through your typed model, explain how you decomposed the foreign contract into records and methods, and identify the observable result that would expose a deficient implementation.",
  "requirements": [
    {
      "capabilityIds": [],
      "essential": true,
      "id": "builds",
      "text": "Build Verisearch.slnx on .NET 10 with nullable analysis and warnings-as-errors enabled."
    },
    {
      "capabilityIds": [
        "language-types-nullability",
        "language-methods-scope",
        "language-classes-records"
      ],
      "essential": true,
      "id": "typed-decomposition",
      "text": "Create sealed StateCommand and CommandResult records inside the supplied Program.cs. Inside CommandContract, implement `StateCommand? Parse(string line, char separator)` and `CommandResult Apply(string input, char separator, string failureMode, string outputOrder)`. Apply must call Parse for each retained segment and return the keys/total/invalid observation that the top-level program writes; dummy members plus a separate inline implementation do not satisfy this public structure contract."
    },
    {
      "capabilityIds": [
        "language-control-flow",
        "language-collections-generics",
        "language-methods-scope"
      ],
      "essential": true,
      "id": "apply-commands",
      "text": "The evaluator supplies at most 10,000 LF-delimited segments that are nonempty before CR handling; larger inputs are outside this contract. LF is the only delimiter and a final unterminated segment is processed. Discard only segments whose length is already zero, then remove one trailing CR from each retained segment; therefore a segment containing only CR becomes an empty malformed command and counts toward the bound, while any other CR is data. Each valid command is exactly SET<separator><key><separator><signed-Int32>: SET is uppercase; key is 1-16 ASCII characters beginning with lowercase a-z and followed only by lowercase a-z, digits, or underscore; the number is an optional ASCII + or - followed by one or more ASCII digits, leading zeroes are allowed, surrounding whitespace and non-ASCII digits are rejected, and the value must fit Int32. There is no quoting or escaping. A later SET replaces that key's value; malformed lines never change accepted state. State must support every valid key, not a fixed pair."
    },
    {
      "capabilityIds": [
        "language-control-flow",
        "language-collections-generics"
      ],
      "essential": true,
      "id": "emit-snapshot",
      "text": "The supplied constants are this candidate's fixed compile-time assignment; no alternate order or runtime selector is required. Sort all currently present keys using exactly ordinal ascending with StringComparer.Ordinal. Write UTF-8 stdout as exactly six lines: `domain=observatory-instrument`, `mode=1`, `order=ordinal ascending`, `keys=` followed by every current key joined by commas without spaces (or exactly `keys=` for empty state), `total=` followed by the checked Int64 sum of current Int32 values, and `invalid=` followed by the malformed-line count for fixed mode 1 or zero for fixed mode 0. Terminate every line, including the sixth, with one LF (U+000A); emit no CR or other text. The 10,000-segment bound makes both the count and Int64 sum non-overflowing."
    }
  ],
  "structuralSignature": "07906d34892c5390100b9c706593275d54ae4ebdd8d67844dc534e3096b20492",
  "title": "observatory-instrument state register",
  "variantId": "foreign-command-state-machine-30ad1c8445d52308",
  "verificationHash": "8bb9b8b4d657d370d5f59f737b7bfd2f3eddda72ca95b33a175022f9cfbdba47",
  "verified": true,
  "version": 1
}