[[lessons]]
id = "s01-l02"
teaches = ["language-namespaces-modularity"]
title = "The shape of an incantation — top-level statements & namespaces"
body = '''

<p>The old masters made you raise a class and a <code>Main</code> method just to utter one line. Modern C# lets <code>Program.cs</code> begin casting from its first rune — these are <strong>top-level statements</strong>:</p>
<pre><code data-kind="runnable">// Complete runnable example
<span class="c">// Program.cs — this IS a complete incantation</span>
Console.WriteLine(<span class="s">"Verisearch v0.1"</span>);</code></pre>
<p>Read that line from the outside inward:</p>
<ol>
<li><code>Console</code> is a type supplied by .NET.</li>
<li>The dot means “use something belonging to that type.”</li>
<li><code>WriteLine</code> is the method being called; the parentheses hold its argument.</li>
<li>The quoted argument is a <code>string</code>, and the final semicolon seals the statement.</li>
</ol>
<div class="callout"><strong>Call versus declaration:</strong> <code>Console.WriteLine("hi");</code> calls an existing method. <code>static void Main(string[] args)</code> declares a method: it gives the method a name, inputs, and return type. On a first pass, recognize the difference; the console lesson ahead forges your first small helper, and Chapter II names that boundary properly.</div>
<p>Behind the veil the compiler still writes the elder form. You will meet it in other wizards' work, so learn its face:</p>
<pre><code data-kind="replacement"><span class="k">namespace</span> Verisearch;

<span class="k">class</span> Program
{
    <span class="k">static void</span> Main(<span class="k">string</span>[] args)
    {
        Console.WriteLine(<span class="s">"Verisearch v0.1"</span>);
    }
}</code></pre>
<ul>
<li><strong>namespace</strong> — a drawer for the names of things. <code>Console</code> truly dwells in the <code>System</code> namespace.</li>
<li><strong>using</strong> — opens a drawer so you may call its contents by short name. Modern workings open the common drawers for you (<code>System</code>, <code>System.Collections.Generic</code>, <code>System.Linq</code>…) via <em>implicit usings</em> — that is why plain <code>Console.WriteLine</code> answers.</li>
<li><strong>statement</strong> — one instruction, sealed with <code>;</code>. <strong>Block</strong> — statements bound together by <code>{ }</code>.</li>
</ul>
<div class="callout">Semicolons and braces are not negotiable. The compiler does not guess. Drop a <code>;</code> and the complaint may point at the NEXT line — look one line up.</div>
<div class="callout csharp-transfer"><p><strong>C#/.NET transfer:</strong> Top-level statements are compiler sugar: C# still emits an entry-point method and permits them in only one source file. Unlike Java, the file need not declare a wrapper class, and unlike Rust, you do not write <code>fn main</code>; namespaces organize type names but do not have to mirror folders.</p></div>
<div class="field-notes"><div class="fn-head">MARGINALIA // THE DEEPER CUT — IN THORNE'S HAND</div>
<p><strong>Comments:</strong> <code>// to the line's end</code> and <code>/* a bound passage */</code>. Comments explain WHY, not what — <code>// increment i</code> above <code>i++</code> is scribble.</p>
<p><strong>Case sensitivity:</strong> <code>Console</code>, <code>console</code>, and <code>CONSOLE</code> are three different names; only the first exists. Most novice "it does not exist" complaints are casing.</p>
<p><strong>The law of names:</strong> letters/digits/underscore, never beginning with a digit, never a keyword. The conventions every guild follows: <code>PascalCase</code> for types/methods/properties, <code>camelCase</code> for locals and parameters.</p>
<p><strong>What the compiler writes for you:</strong> your top-level statements literally become a hidden <code>Main(string[] args)</code> — the <code>args</code> variable is even at hand in top-level runes. One leaf per working may use top-level statements; every other leaf holds types.</p></div>
'''

[[lessons.readings]]
label = "Microsoft Learn — \"C# for Beginners\" video series (ep. 1–5)"
url = "https://learn.microsoft.com/en-us/shows/csharp-for-beginners/"
essential = false

[[lessons.readings]]
label = "freeCodeCamp — C# Tutorial for Beginners (YouTube)"
url = "https://www.youtube.com/watch?v=Yx5HtKNy26k"
essential = false

[[lessons.exercises]]
id = "s01-l02-e1"
type = "mc"
points = 15
required = true
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "explain"
scaffold = "guided"
contextFamily = "verisearch-s01-namespaces-modularity"
aidPolicy = "learning"
prompt = "What are \"top-level statements\"?"
choices = ["Statements that can only appear at the top of a method", "Comments that describe the program", "Code written directly in Program.cs without an explicit class or Main method", "Statements that run with administrator privileges"]
answer = 2
whyWrong = "Top-level statements aren’t about method position or admin rights — they let you write code directly in Program.cs while the compiler synthesizes the class and Main."
hint = "Modern Program.cs begins casting from its first line."
explain = "Top-level statements are ordinary executable statements written directly in <code>Program.cs</code>; the compiler wraps them in the hidden entry-point method for you."

[[lessons.exercises]]
id = "s01-l02-e2"
type = "text"
points = 20
required = true
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "recall"
scaffold = "guided"
contextFamily = "verisearch-s01-namespaces-modularity"
aidPolicy = "learning"
prompt = "Which keyword opens a namespace so you may call its types by short name? (one word)"
answer = "using"
hint = "It appears at the top of leaves: ____ System.Text;"

[[lessons.exercises]]
id = "s01-l02-e3"
type = "mc"
points = 20
required = true
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "explain"
scaffold = "guided"
contextFamily = "verisearch-s01-namespaces-modularity"
aidPolicy = "learning"
prompt = "This inscription is refused by the Forge. Why?"
code = '''
Console.WriteLine("consulting the tomes")
Console.WriteLine("done");'''
choices = ["\"consulting the tomes\" contains a space", "You can't call WriteLine twice", "WriteLine must be lowercase", "The first line is missing its semicolon"]
answer = 3
whyWrong = "The space in the string and calling WriteLine twice are both fine; C# statements end in ‘;’, so the first line’s missing semicolon is what the compiler rejects."
hint = "Every statement must be sealed with the same character."
explain = "The first <code>WriteLine</code> call needs a trailing semicolon. The compiler may complain at the next line because that is where it finally proves the previous statement never ended."

[[lessons.exercises]]
id = "s01-l02-d1"
type = "type"
points = 12
required = false
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "complete"
scaffold = "worked"
contextFamily = "verisearch-s01-namespaces-modularity"
aidPolicy = "learning"
prompt = "Copy the elder explicit form once — you will know its face in every older grimoire and codebase."
code = '''
namespace Verisearch;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Verisearch v0.1");
    }
}'''

[[lessons.exercises]]
id = "s01-l02-w1"
type = "write"
points = 30
required = true
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "build"
scaffold = "guided"
contextFamily = "verisearch-s01-namespaces-modularity"
aidPolicy = "learning"
prompt = "Write an incantation that utters these two lines, in this order."
expect = '''
the candle is lit
the tome lies open'''
solution = '''
Console.WriteLine("the candle is lit");
Console.WriteLine("the tome lies open");
'''
hint = "Two WriteLine calls. Order of statements = order of utterance."

[[lessons.concepts]]
id = "language-namespaces-modularity"
purpose = "Read a C# file as a named unit of code so a growing program can be divided into namespaces instead of one anonymous scroll."
anatomy = "Top-level statements are the compiler-written entry point; a namespace declaration names the group a file contributes to, and using directives bring other namespaces into scope."
example = "A one-line Program.cs shown beside the same behavior spelled out as namespace Verisearch with a class Program and a Main method."
observable = "Both spellings build and print the same line, and a type moved under a namespace stops resolving until its name or a using is supplied."
failure = "Pasting a namespace-wrapped example on top of surviving top-level statements declares two entry points, and the resulting build error hides the edit that caused it."
practice = "s01-l02-e1"
