[freestyle]
title = "THE WORKING: The Skeleton of the Tome"
requires = ["classes-objects", "properties-constructors", "records", "enums", "linq-basics", "canonical-models"]
brief = "Create the one model file the rest of Verisearch will share.<ul><li>Add <code>Models.cs</code> containing exactly the workshop's <code>SourceKind</code>, <code>SourceSpec</code>, <code>SourceDocument</code>, <code>Passage</code>, <code>QuoteProposal</code>, <code>QuoteBatch</code>, and <code>VerifiedQuote</code></li><li>Delete or rename any earlier syntax specimen that collides; do not keep two meanings for <code>Source</code>, <code>Passage</code>, or a proposal</li><li>In <code>Program.cs</code>, add a temporary <code>demo</code> branch that constructs two SourceSpecs and three Passages, filters passages shorter than 20 characters with LINQ, and prints <code>&lt;id&gt;: &lt;label&gt;</code></li><li>Keep the Chapter I loop and Chapter II question boundary intact</li><li>Run <code>dotnet build</code>, then exercise both <code>demo</code> and an ordinary question</li></ul><div class=\"callout\"><strong>Canonical means future chapters edit these records deliberately.</strong> Examples in earlier lessons taught syntax; <code>Models.cs</code> now owns the product contract.</div>"
reward = 300
packages = []
xray = "THORNE compiles every file together, rejects duplicate demonstration types, verifies QuoteProposal has no URL field, and checks that VerifiedQuote carries C#-owned source label and URL. He runs the demo through the existing loop and expects the LINQ result to be computed from the list."

[freestyle.badge]
id = "badge-s03"
name = "SHAPER OF FORMS"
desc = "Raised the skeleton of forms the whole tome hangs on."

[[freestyle.rubric]]
criterion = "Compiles & runs"
weight = 20
desc = "Builds clean; demo output renders."

[[freestyle.rubric]]
criterion = "Canonical records & enum"
weight = 25
desc = "Models.cs contains the specified source, passage, proposal, batch, and verified records plus SourceKind with stable names."

[[freestyle.rubric]]
criterion = "Verification boundary in types"
weight = 20
desc = "Raw model proposals cannot carry attribution and cannot be passed off as VerifiedQuote without an explicit verification step."

[[freestyle.rubric]]
criterion = "LINQ demo"
weight = 15
desc = "Where/OrderBy chain filters short passages and preserves stable id order from the constructed list."

[[freestyle.rubric]]
criterion = "Organization & readability"
weight = 20
desc = "One type per file (or close), consistent naming (PascalCase types/properties), no dead code."
