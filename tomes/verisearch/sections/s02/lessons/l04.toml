[[lessons]]
id = "s02-l04"
teaches = ["lists-arrays"]
title = "Arrays & List<T> — things in their order"
body = '''

<p><strong>Arrays</strong> are cast at a fixed size. Once made, they cannot grow:</p>
<pre><code><span class="k">string</span>[] sourceLabels = { <span class="s">"RFC"</span>, <span class="s">"NIST"</span> };
sourceLabels[0] = <span class="s">"RFC 8446"</span>;   <span class="c">// exchange one thing: fine</span>
<span class="c">// sourceLabels.Add(...)  ← no such rite. Arrays do not grow.</span></code></pre>
<p><strong><code>List&lt;T&gt;</code></strong> is the growing array you will truly use. The <code>&lt;T&gt;</code> is a <em>generic type parameter</em> — a List of what?</p>
<pre><code>List&lt;<span class="k">string</span>&gt; passages = <span class="k">new</span>();
passages.Add(<span class="s">"TLS protects application data."</span>);
passages.Add(<span class="s">"A second source passage."</span>);
passages.RemoveAt(1);
Console.WriteLine(passages.Count);        <span class="c">// 1  (List keeps Count, arrays keep Length)</span>
Console.WriteLine(passages.Contains(<span class="s">"TLS protects application data."</span>)); <span class="c">// True</span>
Console.WriteLine(passages[0]);           <span class="c">// the same [index] reach as arrays</span></code></pre>
<p>The string→vessel move used for conservative whitespace normalization is <code>Split</code>:</p>
<pre><code><span class="k">string</span> query = <span class="s">"What are the TLS 1.3 cipher suites?"</span>;
<span class="k">string</span>[] tokens = query.Split(<span class="s">' '</span>);   <span class="c">// ["What","are","the","TLS","1.3","cipher","suites?"]</span></code></pre>
<p>Its mirror, <code>string.Join</code>, puts the complete question back together after empty whitespace entries are removed:</p>
<pre><code><span class="k">string</span> normalized = <span class="k">string</span>.Join(<span class="s">" "</span>, tokens);</code></pre>
<div class="callout">The rule of thumb: parameters and fixed matter → arrays serve. Anything you gather as you go → <code>List&lt;T&gt;</code>.</div><div class="field-notes"><div class="fn-head">MARGINALIA // THE DEEPER CUT — IN THORNE'S HAND</div>
<p><strong>The initializer forms:</strong> <code>int[] a = { 1, 2, 3 };</code> · <code>var list = new List&lt;string&gt; { "a", "b" };</code> · an empty list: <code>List&lt;string&gt; l = new();</code> or the newest collection expression <code>List&lt;int&gt; l = [1, 2, 3];</code>.</p>
<p><strong>Split, done properly:</strong> <code>text.Split(' ')</code> yields empty entries for doubled spaces. The input boundary uses <code>text.Split(' ', StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries)</code>, then joins every token again. This is formatting cleanup, not word selection.</p>
<p><strong>Other List workings:</strong> <code>Insert(0, item)</code> set before all, <code>list[^1]</code> the last thing (index-from-end), <code>list.GetRange(0, 3)</code> the first three, <code>Clear()</code>.</p></div>'''

[[lessons.readings]]
label = "Microsoft Learn — \"Collections\""
url = "https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/collections"
essential = false

[[lessons.readings]]
label = "Tim Corey — C# Collections (YouTube)"
url = "https://www.youtube.com/watch?v=Db9141a4t74"
essential = false

[[lessons.exercises]]
id = "s02-l04-e1"
type = "mc"
points = 20
prompt = "You are gathering an unknown number of extracted passages from a page. The best vessel?"
choices = ["string[]", "int[]", "string", "List<string>"]
answer = 3
whyWrong = "A fixed array needs a known size and int[]/plain string are the wrong shape — List<string> grows as you add an unknown number of items."
hint = "It must grow as you .Add()."
explain = "A <code>List&lt;string&gt;</code> holds strings and can grow with <code>Add</code>, so it fits a collection whose final size is not known in advance."

[[lessons.exercises]]
id = "s02-l04-e2"
type = "text"
points = 20
prompt = "Arrays keep <code>.Length</code>. What is the like property on a <code>List&lt;T&gt;</code>?"
answer = "Count"
accept = [".count"]
hint = "Also what a certain pale nobleman is called."

[[lessons.exercises]]
id = "s02-l04-e3"
type = "fill"
points = 25
prompt = "Complete the rune to break the asking into space-parted tokens."
code = '''
string query = "TLS 1.3 cipher suites";
string[] tokens = query.____(' ');'''
answer = "Split"
hint = "The mirror of string.Join."

[[lessons.exercises]]
id = "s02-l04-e4"
type = "mc"
points = 20
prompt = "What does <code>string.Join(\" + \", new[] { \"a\", \"b\", \"c\" })</code> yield?"
choices = ["\"a + b + c\"", "\"a+b+c\"", "\" + a + b + c + \"", "\"abc\""]
answer = 0
whyWrong = "Join inserts the separator only BETWEEN elements, giving ‘a + b + c’ — no leading/trailing separator, and it isn’t bare concatenation."
hint = "The separator stands BETWEEN things only."
explain = "<code>string.Join</code> places the supplied separator between adjacent elements, producing <code>a + b + c</code> with no separator at either end."

[[lessons.exercises]]
id = "s02-l04-d1"
type = "type"
points = 14
reps = 2
prompt = "The master-grade Split — copy it twice; you will wield it in this chapter's Great Working."
code = '''
string[] tokens = query.Split(' ',
    StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries);'''

[[lessons.exercises]]
id = "s02-l04-w1"
type = "write"
points = 34
prompt = "Hear one line by whisper. Split it into words, utter the word COUNT first, then each word on its own line."
stdin = "trust the ietf standards"
expect = '''
4
trust
the
ietf
standards'''
solution = '''
string line = Console.ReadLine() ?? "";
string[] words = line.Split(' ', StringSplitOptions.RemoveEmptyEntries);
Console.WriteLine(words.Length);
foreach (string word in words) Console.WriteLine(word);
'''
hint = "Split with RemoveEmptyEntries, print tokens.Length, then foreach."
