[[lessons]]
id = "s11-l03"
teaches = ["language-namespaces-modularity"]
title = "Pipeline — One Method That Tells the Truth"
body = '''

<p>The pipeline is a behavioral sequence, not necessarily one class or one giant method. Its job is to preserve contracts across source selection, concurrent reading, extraction, semantic AI selection, deterministic verification, and reporting.</p>
<p>Write the sequence as arrows before coding. Mark which values are trusted, which are external, and where cancellation or partial failure may occur. The AI must receive the whole question and the stable passages from the sources the user selected. Its output remains proposals. Only the verifier may pair exact quote text with source-owned attribution.</p>
<pre><code data-kind="pseudocode">selected sources -&gt; read outcomes -&gt; stable passages
whole question + stable passages -&gt; AI proposals
proposals + same passages -&gt; verified quotations -&gt; report</code></pre>
<p>This is pseudocode architecture. You may implement a coordinator, compose functions, use application services, or choose another understandable structure. Keep the composition root at the outer edge so provider and infrastructure choices do not leak into domain verification.</p>
<p>Decide how the program behaves when all sources fail, some fail, no passages remain, the AI returns malformed JSON, every proposal is rejected, or cancellation arrives. Ordinary source failures can coexist with successful evidence. AI unavailability is not permission to run a non-AI relevance algorithm. Exact verification has no fuzzy or case-insensitive fallback unless your product explicitly changes its claim—and this tome's claim is ordinal source containment.</p>
<p>Build an integration fixture using test doubles for I/O and AI. Feed a punctuation-bearing whole question, two sources, one controlled failure, one exact proposal, and one fabricated proposal. Assert calls and final output data. This fixture becomes the rehearsal for the independent Working, not a substitute for a real configured AI run later.</p>
<div class="callout"><p>The late performance is graded on reconciled behavior and C# capability use, not resemblance to the hidden replay architecture.</p></div>
<div class="callout csharp-transfer"><p><strong>C#/.NET transfer:</strong> The pipeline can compose <code>Task&lt;T&gt;</code>, records, interfaces, LINQ, and pattern matching without a special application framework. The C# type graph should make each transition visible; the composition root selects implementations while the pipeline consumes contracts.</p></div>
<div class="field-notes"><div class="fn-head">FIELD NOTES // DRAW DEPENDENCY ARROWS</div><p>Domain verification should not point toward a CLI process, HTTP client, console library, or environment variable. Those arrows belong at adapters and composition.</p></div>
'''

[[lessons.readings]]
label = "Microsoft Learn — Async return types"
url = "https://learn.microsoft.com/en-us/dotnet/csharp/asynchronous-programming/async-return-types"
essential = false

[[lessons.exercises]]
id = "s11-l03-e1"
type = "mc"
points = 25
required = true
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "predict"
scaffold = "guided"
contextFamily = "verisearch-s11-namespaces-modularity"
aidPolicy = "limited"
prompt = "Why does RunAsync return PipelineResult instead of printing?"
choices = ["Console is too slow", "Async methods cannot print", "Records render automatically", "Computation stays testable and presentation can change independently"]
answer = 3
whyWrong = "Console speed, async restrictions, and automatic rendering are not the reason. Returning data separates facts from their presentation."
explain = "Tests can inspect quotes, errors, and counts without scraping terminal output."
hint = "Which Chapter X class owns the face?"

[[lessons.exercises]]
id = "s11-l03-e2"
type = "mc"
points = 20
required = true
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "explain"
scaffold = "guided"
contextFamily = "verisearch-s11-namespaces-modularity"
aidPolicy = "limited"
prompt = "One fetched page produces no usable passages. What should the orchestrator do?"
choices = ["Record that source error and continue with other sources", "Send empty HTML to the configured AI", "Invent a passage", "Crash every source"]
answer = 0
whyWrong = "The model cannot use an empty batch, invention breaks truth, and a page-local problem should not erase healthy sources."
explain = "The reason remains visible in SourceErrors while the run continues."
hint = "Match failure scope to containment scope."

[[lessons.exercises]]
id = "s11-l03-e3"
type = "mc"
points = 20
required = true
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "explain"
scaffold = "guided"
contextFamily = "verisearch-s11-namespaces-modularity"
aidPolicy = "limited"
prompt = "What parameter travels through fetching and model selection for coordinated shutdown?"
choices = ["A global Boolean", "CancellationToken", "An output string", "Thread.Abort"]
answer = 1
whyWrong = "Global flags and strings are not the .NET cancellation contract, while Thread.Abort is not the cooperative pattern."
explain = "One token lets Ctrl+C or a deadline stop all participating asynchronous operations."
hint = "It is the last parameter on the async contracts."

[[lessons.exercises]]
id = "s11-l03-d1"
type = "type"
points = 14
required = false
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "complete"
scaffold = "worked"
contextFamily = "verisearch-s11-namespaces-modularity"
aidPolicy = "limited"
reps = 2
prompt = "Retype the proposal-to-proof handoff twice."
code = '''QuoteBatch proposals = await _selector.SelectAsync(question, passages, ct);
VerificationBatch proof = _verifier.Verify(proposals.Quotes, passages, remaining);
quotes.AddRange(proof.Accepted);
rejected += proof.Rejected;'''

[[lessons.exercises]]
id = "s11-l03-w1"
type = "write"
points = 36
required = true
capabilities = ["language-namespaces-modularity"]
cognitiveTask = "integrate"
scaffold = "independent"
contextFamily = "verisearch-s11-namespaces-modularity"
aidPolicy = "limited"
prompt = "Write <code>RunPipeline</code> so an empty source list returns empty: no configured sources and a three-source list returns ok: 3 sources. Print both calls."
starter = '''Console.WriteLine("NOT ORCHESTRATED");
'''
expect = '''empty: no configured sources
ok: 3 sources'''
solution = '''Console.WriteLine(RunPipeline(new List<string>()));
Console.WriteLine(RunPipeline(new List<string> { "a", "b", "c" }));

static string RunPipeline(List<string> sources)
{
    if (sources.Count == 0)
    {
        return "empty: no configured sources";
    }
    return $"ok: {sources.Count} sources";
}
'''
hint = "Use a Count == 0 guard clause, then return the happy-path count."

[[lessons.concepts]]
id = "language-namespaces-modularity"
purpose = "Split a growing C# program into discoverable files and namespaces with one clear owner per responsibility."
anatomy = "Place related types under a namespace, keep the entrypoint as composition, and reference public contracts instead of copying declarations."
example = "The disposable examples in Pipeline — One Method That Tells the Truth isolate this capability before the learner chooses how it belongs in the cumulative project. The lesson Pipeline — One Method That Tells the Truth supplies the disposable observation point."
observable = "The whole solution builds with one definition for each concept and dependencies point in an understandable direction. Confirm it while practicing Pipeline — One Method That Tells the Truth."
failure = "Duplicate types, circular ownership, or business logic in Program.cs makes later integration brittle."
practice = "s11-l03-e1"
