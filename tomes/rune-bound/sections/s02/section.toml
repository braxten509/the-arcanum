id = "s02"
codename = "CHART II // THE SHAPE OF THINGS"
short = "Shape Ledger"
title = "The Shape of Things"
build = "Model RuneBound entities with classes, methods, inheritance, dictionaries, and JSON-backed rules."
brief = '''
<p>The second chart gives the map's facts durable shapes. A GameEntity carries position and size, child classes add meaning without copying the base, and dictionaries keep tile rules separate from the code that uses them.</p><ul><li>construct objects with explicit attributes and methods</li><li>extend a shared entity without losing its contract</li><li>look up data by key and load structured JSON</li></ul>
'''

