id = "s02"
codename = "CHART II // THE SHAPE LEDGER"
short = "Shape Ledger"
title = "The Shape Ledger"
build = "Model RuneBound things with classes, look up rules by key, and preserve a tiny object state as JSON."
brief = '''
<p>Before the window opens, a wayfinder needs durable names for the things that will inhabit it. Shape entities with positions and sizes, put tile and item rules behind dictionary keys, then prove that a small record can survive a JSON round trip.</p><ul><li>give every entity its own coordinate and size state</li><li>use a dictionary for data lookup instead of branching rules through the program</li><li>keep lists owned by each inventory and serialize simple instance state deliberately</li></ul>
'''
