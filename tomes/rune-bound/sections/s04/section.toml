id = "s04"
codename = "CHART IV // THE GROUND BENEATH"
short = "Ground Survey"
title = "The Ground Beneath"
build = "Create data-defined CSV tiles, tilemap.py, world-to-screen rendering, and held-key camera scrolling in main.py."
brief = '''
<p>Wayfinder, a colored rectangle proved that the window works. It did not prove that RuneBound has a world. In this chart you replace that probe with a map whose names, colors, and solid rules live in files, not scattered through <code>main.py</code>.</p><ul><li>create <code>data/tiles.json</code>, a CSV overworld, and <code>tilemap.py</code></li><li>keep <code>TileMap</code> as owner of map data and painting while <code>main.py</code> pumps events and chooses camera state</li><li>run <code>python3 main.py</code> and scroll a colored 25×19 world with arrow keys</li></ul><p>A camera changes screen coordinates only. It never rewrites a tile’s world position. Keep every map cell as a lower-case string id and keep row before column.</p>
'''
