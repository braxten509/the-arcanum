id = "s03"
codename = "CHART III // A WINDOW ON THE WORLD"
short = "World Window"
title = "A Window on the World"
build = "Create main.py: a 800×600 PyGame window, responsive frame loop, painted probe, and 60 FPS clock."
brief = '''
<p>Wayfinder, the terminal survey has done its work. This chart raises RuneBound’s first real window: a surface that the operating system can show, repaint, and close. You will build the small loop every later system enters—events first, then changes, then drawing, then the clock.</p><ul><li>verify that your local Python can import PyGame, then create and run <code>main.py</code> from its own folder</li><li>open a 800×600 window, draw a visible grass field and amber waystone, and close through the window’s X button</li><li>preserve the four-part frame route: handle events, update state, draw, flip, and cap the frame rate</li></ul><p>The old terminal grid prints and <code>test_entity.py</code> are retired here. Their parser and model contracts survive; their isolated proof route does not.</p>
'''
