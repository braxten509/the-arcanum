# Section s03 — INSCRIPTION III // THE PARSER'S EYE
id = "s03"
codename = "INSCRIPTION III // THE PARSER'S EYE"
short = "The Parser's Eye"
title = "The Parser's Eye"
build = "Lector tokenizes template directives ({{ ... }} blocks). Teaches new/delete, std::unique_ptr, std::shared_ptr, RAII, move semantics fundamentals, ownership. First concrete lifetime bug students must debug."
brief = "<p>The sigil-press must recognise its own marks. Every <code>{% raw %}{{ ... }}{% endraw %}</code> directive is a spell the press must parse — but who owns the ink? RAII, smart pointers, and the first taste of move semantics: you will debug your first lifetime leak here.</p>"
