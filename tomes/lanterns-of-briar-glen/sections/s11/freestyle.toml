[freestyle]
title = "THE WORKING: BOUNDARIES IN THE GROVE"
brief = '''<p>Rowan asks you to move the proven text trail out of its first shelter without changing its story. You will author the canonical <code>briar_glen</code> package yourself; no starter implementation is supplied.</p><ul><li>Create <code>briar_glen/__init__.py</code>, <code>briar_glen/domain.py</code>, and <code>briar_glen/main.py</code>. Keep the initializer behavior-free. Put the existing composed Inventory, QuestState, Player, validation, recovery, command helpers, and deterministic six-turn trail in the domain module. Let the entry module invoke that domain behavior rather than owning another controller.</li><li>Move—do not duplicate—the canonical behavior out of <code>game.py</code>. Update <code>tests/test_game.py</code> so its existing 11 arrange/act/assert checks import their seams from the domain module. Test collection must not run the whole trail.</li><li>From the external project root with <code>.venv</code> active, run <code>python -m briar_glen.main</code> and preserve the established visible route, rejected invalid command, final health of <code>12</code>, key discovery, and six completed turns. Run <code>python -m pytest -q</code> until it reports <code>11 passed</code>.</li><li>Only after both replacement checks are clean, retire the exact root <code>game.py</code> prototype and confirm it is absent. Rerun the package module entry and tests after retirement. Do not add a public export list, guarded-entry policy, command-line parsing, type hints, files, JSON, Pygame, packaging metadata, assets, or a second game.</li></ul>'''
requires = ["package-file-layout", "domain-module-extraction", "module-entrypoint-parity", "language-module-package-boundaries", "language-functions-decomposition", "language-test-verification", "language-class-object-abstraction", "language-composition-design"]
mechanisms = ["terminal-print-working-directory", "terminal-change-directory", "terminal-check-python-version", "venv-create-environment", "venv-activate-environment", "venv-resolve-active-python", "editor-create-file", "editor-edit-file", "editor-save-file", "terminal-read-saved-file", "terminal-invoke-saved-file", "python-literal-values", "python-name-binding", "python-arithmetic-operators", "python-comparison-operators", "python-boolean-operators", "python-print-call", "python-string-literal", "python-string-concatenation", "python-f-string", "string-strip-method", "string-lower-method", "string-split-method", "python-list-literal", "python-list-indexing", "list-append-method", "python-dict-literal", "python-dict-key-access", "dict-get-method", "python-set-literal", "python-membership-operators", "set-add-method", "python-tuple-literal", "collection-copy-method", "python-if-statement", "python-elif-clause", "python-else-clause", "python-for-statement", "python-in-loop-target", "python-range-call", "python-while-statement", "python-break-statement", "python-continue-statement", "bounded-loop-invariant", "python-def-statement", "python-function-call", "python-parameters-arguments", "python-return-statement", "pure-function-boundary", "mutating-function-boundary", "pip-install-exact-spec", "pytest-module-invocation", "python-import-statement", "pytest-test-function", "python-assert-statement", "arrange-act-assert", "pytest-failure-location", "red-green-rerun-cycle", "python-exception-types", "python-raise-statement", "python-try-statement", "python-except-clause", "python-else-exception-clause", "exception-recovery-boundary", "exception-propagation", "python-finally-clause", "python-class-statement", "python-init-method", "python-self-parameter", "python-instance-construction", "python-instance-attribute", "python-instance-method", "python-identity-operator", "collaborating-object-responsibility", "constructor-dependency-injection", "object-ownership", "method-delegation", "composition-over-inheritance", "collaborator-test-seam", "terminal-create-package-directories", "python-package-directory", "python-init-module", "python-module-file", "entry-domain-module-separation", "python-module-execution", "prototype-retirement"]
reward = 240
xray = "The grader traces one canonical trail path: a quiet package initializer, domain-owned composed rules, and a narrow module entry that starts that same behavior. It expects tests to import the domain rather than a retired root prototype, 11 focused passing checks, the preserved six-turn transcript, and game.py absent only after those checks pass. Dock copied domain logic, import-time trail execution, a second controller, a stale game.py, premature public-import policy or CLI work, lost error recovery, or source-text-only tests. Reward exact package paths, one-way entry-to-domain flow, descriptive snake_case names, four-space indentation, and focused arrange/act/assert spacing."

[[freestyle.referenceSteps]]
id = "s11-write-package-initializer"
path = "briar_glen/__init__.py"
mode = "write"
mechanisms = ["python-package-directory", "python-init-module"]
instruction = "Private replay creates the learner-owned, behavior-free package initializer."
content = ''''''

[[freestyle.referenceSteps]]
id = "s11-write-domain-module"
path = "briar_glen/domain.py"
mode = "write"
mechanisms = ["terminal-print-working-directory", "terminal-change-directory", "terminal-check-python-version", "venv-create-environment", "venv-activate-environment", "venv-resolve-active-python", "editor-create-file", "editor-edit-file", "editor-save-file", "terminal-read-saved-file", "terminal-invoke-saved-file", "python-literal-values", "python-name-binding", "python-arithmetic-operators", "python-comparison-operators", "python-boolean-operators", "python-print-call", "python-string-literal", "python-string-concatenation", "python-f-string", "string-strip-method", "string-lower-method", "string-split-method", "python-list-literal", "python-list-indexing", "list-append-method", "python-dict-literal", "python-dict-key-access", "dict-get-method", "python-set-literal", "python-membership-operators", "set-add-method", "python-tuple-literal", "collection-copy-method", "python-if-statement", "python-elif-clause", "python-else-clause", "python-for-statement", "python-in-loop-target", "python-range-call", "python-while-statement", "python-break-statement", "python-continue-statement", "bounded-loop-invariant", "python-def-statement", "python-function-call", "python-parameters-arguments", "python-return-statement", "pure-function-boundary", "mutating-function-boundary", "pip-install-exact-spec", "pytest-module-invocation", "python-import-statement", "pytest-test-function", "python-assert-statement", "arrange-act-assert", "pytest-failure-location", "red-green-rerun-cycle", "python-exception-types", "python-raise-statement", "python-try-statement", "python-except-clause", "python-else-exception-clause", "exception-recovery-boundary", "exception-propagation", "python-finally-clause", "python-class-statement", "python-init-method", "python-self-parameter", "python-instance-construction", "python-instance-attribute", "python-instance-method", "python-identity-operator", "collaborating-object-responsibility", "constructor-dependency-injection", "object-ownership", "method-delegation", "composition-over-inheritance", "collaborator-test-seam", "terminal-create-package-directories", "python-package-directory", "python-init-module", "python-module-file", "entry-domain-module-separation", "python-module-execution", "prototype-retirement"]
instruction = "Private replay moves the composed trail rules and deterministic controller into their one canonical domain module."
content = '''class Inventory:
    def __init__(self, items):
        self.items = items

    def add_item(self, item):
        if item not in self.items:
            self.items.append(item)


class QuestState:
    def __init__(self, discoveries):
        self.discoveries = discoveries
        self.path_open = False

    def mark_discovery(self, discovery):
        self.discoveries.add(discovery)

    def search_briar(self):
        search_marks = ["thorns", "old ribbon", "lantern key", "deep mud"]
        search_index = 0
        while search_index < 4:
            search_mark = search_marks[search_index]
            search_index = search_index + 1
            if search_mark == "old ribbon":
                continue
            print("Search:", search_mark)
            if search_mark == "lantern key":
                self.mark_discovery("lantern key")
                print("Lantern key found.")
                break
        self.path_open = "lantern key" in self.discoveries
        return self.path_open


class Player:
    def __init__(self, name, health, inventory, quest_state):
        if health < 0:
            raise ValueError("Starting health cannot be below zero")
        self.name = name
        self.health = health
        self.inventory = inventory
        self.quest_state = quest_state

    def rest(self):
        self.health = self.health + 2
        return self.health

    def pack_item(self, item):
        self.inventory.add_item(item)

    def search_quest(self):
        return self.quest_state.search_briar()


def turn_line(turn_number, command):
    return f"Turn {turn_number}: {command}"


def has_lantern_key(quest_state):
    return "lantern key" in quest_state.discoveries


def validate_command(command):
    allowed_commands = [
        "look waystone",
        "take moonleaf",
        "rest",
        "search briar",
        "leave",
    ]
    if command not in allowed_commands:
        raise ValueError("Unknown command: " + command)
    return command


def show_opening(status, command, parts, player, coordinate):
    print(status)
    print("Normalized command:", command)
    print("Command words:", parts)
    print("Pack before trail:", player.inventory.items)
    print("First packed item:", player.inventory.items[0])
    print("Player health:", player.health)
    player.quest_state.mark_discovery("north gate")
    print("Known north gate:", "north gate" in player.quest_state.discoveries)
    print("Coordinates:", coordinate)


def handle_command(command, player):
    validate_command(command)
    route_result = {"path_open": False, "keep_traveling": True}
    if command == "look waystone":
        player.quest_state.mark_discovery("waystone")
        print("The waystone points north.")
    elif command == "take moonleaf":
        player.pack_item("moonleaf")
        print("Moonleaf packed.")
    elif command == "rest":
        print(f"Health restored: {player.rest()}")
    elif command == "search briar":
        route_result["path_open"] = player.search_quest()
    elif command == "leave":
        print("Trail closed.")
        route_result["keep_traveling"] = False
    return route_result


def run_turn(command, player, completed_commands):
    try:
        route_result = handle_command(command, player)
    except ValueError as error:
        print("Invalid command:", error)
        return {"path_open": False, "keep_traveling": True}
    else:
        return route_result
    finally:
        completed_commands.append(command)
        print("Turn cleanup:", command)


def run_trail():
    health = 10
    lantern_oil = 3
    remaining_oil = lantern_oil - 1
    lantern_lit = health > 0 and remaining_oil > 0
    traveler_name = "Mira"
    status_lead = "Lantern status for " + traveler_name
    status_line = f"{status_lead}: {health} health, {remaining_oil} oil, lit={lantern_lit}"
    raw_command = "  LOOK WAYSTONE  "
    normalized_command = raw_command.strip().lower()
    command_parts = normalized_command.split()
    inventory = Inventory(["waystone map", "rations"])
    inventory_snapshot = inventory.items.copy()
    quest_state = QuestState({"north gate"})
    camp_coordinate = (4, 7)
    player = Player(traveler_name, health, inventory, quest_state)
    commands = [normalized_command, "take moonleaf", "rest", "search briar", "sing", "leave"]
    turn_limit = 6
    turns_taken = 0
    path_open = False
    completed_commands = []

    show_opening(status_line, normalized_command, command_parts, player, camp_coordinate)

    for turn_index in range(turn_limit):
        command = commands[turn_index]
        turns_taken = turns_taken + 1
        print(turn_line(turns_taken, command))
        route_result = run_turn(command, player, completed_commands)
        path_open = route_result["path_open"]
        if route_result["keep_traveling"] == False:
            break

    print("Turns taken:", turns_taken)
    print("Pack after trail:", player.inventory.items)
    print("Snapshot before trail:", inventory_snapshot)
    print("Found lantern key:", has_lantern_key(player.quest_state))
    print("Path open:", path_open)
    print("Final health:", player.health)
    print("Completed turns:", completed_commands)'''

[[freestyle.referenceSteps]]
id = "s11-write-module-entry"
path = "briar_glen/main.py"
mode = "rewrite"
preserves = "all-active"
mechanisms = ["python-module-execution", "entry-domain-module-separation", "python-import-statement", "python-function-call"]
instruction = "Private replay writes the narrow entry that starts the domain trail in both module and saved-entry execution contexts."
content = '''try:
    from briar_glen.domain import run_trail
except ModuleNotFoundError:
    from domain import run_trail


run_trail()'''

[[freestyle.referenceSteps]]
id = "s11-replace-domain-regression-tests"
path = "tests/test_game.py"
mode = "replace"
find = '''from game import Inventory, Player, QuestState, handle_command, run_turn, turn_line'''
mechanisms = ["entry-domain-module-separation", "python-import-statement", "collaborator-test-seam", "pytest-test-function", "python-assert-statement", "arrange-act-assert"]
instruction = "Private replay preserves the focused composed tests while directing them at the extracted domain module."
content = '''from briar_glen.domain import Inventory, Player, QuestState, handle_command, run_turn, turn_line


def make_player():
    inventory = Inventory([])
    quest_state = QuestState(set())
    return Player("Mira", 10, inventory, quest_state)


def test_turn_line_returns_numbered_command():
    actual_line = turn_line(3, "rest")

    assert actual_line == "Turn 3: rest"


def test_rest_command_changes_supplied_player_and_keeps_traveling():
    player = make_player()

    route_result = handle_command("rest", player)

    assert player.health == 12
    assert route_result == {"path_open": False, "keep_traveling": True}


def test_invalid_command_raises_value_error_before_collaborator_mutation():
    player = make_player()
    was_rejected = False

    try:
        handle_command("sing", player)
    except ValueError:
        was_rejected = True

    assert was_rejected == True
    assert player.health == 10
    assert player.inventory.items == []
    assert player.quest_state.discoveries == set()


def test_invalid_turn_recovers_and_records_completion():
    player = make_player()
    completed_commands = []

    route_result = run_turn("sing", player, completed_commands)

    assert route_result == {"path_open": False, "keep_traveling": True}
    assert player.inventory.items == []
    assert player.quest_state.discoveries == set()
    assert completed_commands == ["sing"]


def test_successful_turn_records_completion():
    player = make_player()
    completed_commands = []

    route_result = run_turn("rest", player, completed_commands)

    assert route_result == {"path_open": False, "keep_traveling": True}
    assert player.health == 12
    assert completed_commands == ["rest"]


def test_player_construction_exposes_attributes_and_collaborators():
    player = make_player()

    assert player.name == "Mira"
    assert player.health == 10
    assert player.inventory.items == []
    assert player.quest_state.discoveries == set()


def test_player_rest_returns_health_and_rejects_negative_start():
    player = make_player()
    was_rejected = False

    result = player.rest()
    try:
        Player("Mira", -1, Inventory([]), QuestState(set()))
    except ValueError:
        was_rejected = True

    assert result == 12
    assert player.health == 12
    assert was_rejected == True


def test_player_identity_distinguishes_alias_and_new_instance():
    player = make_player()
    alias = player
    matching_player = make_player()

    assert alias is player
    assert matching_player is not player


def test_inventory_owns_item_mutation():
    inventory = Inventory([])

    inventory.add_item("moonleaf")

    assert inventory.items == ["moonleaf"]


def test_quest_state_owns_briar_route_change():
    quest_state = QuestState(set())

    path_open = quest_state.search_briar()

    assert "lantern key" in quest_state.discoveries
    assert quest_state.path_open == True
    assert path_open == True


def test_command_delegates_pack_action_to_supplied_inventory():
    player = make_player()

    handle_command("take moonleaf", player)

    assert player.inventory.items == ["moonleaf"]
    assert player.health == 10'''

[freestyle.badge]
id = "badge-s11"
name = "GROVE CARTOGRAPHER"
desc = "Moved Briar Glen into one testable package without losing its trail."

[[freestyle.rubric]]
criterion = "Package Tree and Module Roles"
weight = 22
mechanisms = ["terminal-create-package-directories", "python-package-directory", "python-init-module", "python-module-file", "entry-domain-module-separation"]
desc = "briar_glen contains a behavior-free initializer, domain.py owns reusable composed trail behavior, and main.py is a narrow entry rather than a second controller."

[[freestyle.rubric]]
criterion = "Preserved Domain and Tests"
weight = 25
mechanisms = ["python-import-statement", "entry-domain-module-separation", "collaborator-test-seam", "pytest-test-function", "python-assert-statement", "arrange-act-assert", "pytest-module-invocation"]
desc = "tests/test_game.py imports the extracted domain, preserves all 11 focused arrange/act/assert checks, and does not run the whole trail while it is collected."

[[freestyle.rubric]]
criterion = "Entry Parity and Retirement"
weight = 25
mechanisms = ["python-module-execution", "prototype-retirement", "terminal-invoke-saved-file"]
desc = "python -m briar_glen.main preserves the established six-turn observations and works after the exact root game.py prototype is absent."

[[freestyle.rubric]]
criterion = "Prior Trail Contracts"
weight = 18
mechanisms = ["exception-recovery-boundary", "bounded-loop-invariant", "composition-over-inheritance", "method-delegation"]
desc = "The extracted trail preserves composed ownership, ValueError-before-mutation, narrow recovery, one completion entry per attempted command, and the deterministic final health, discovery, and turn evidence."

[[freestyle.rubric]]
criterion = "Python Boundary Craft"
weight = 10
mechanisms = ["editor-edit-file", "editor-save-file", "terminal-read-saved-file"]
desc = "Saved package and test files use descriptive snake_case names, four-space indentation, two blank lines between top-level definitions, and a clear one-way entry-to-domain reading path."
