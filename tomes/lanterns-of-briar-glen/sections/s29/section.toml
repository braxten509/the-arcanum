# Section s29 — scaffolded by tools/new_tome.py. Replace every TODO.
# One lesson with one of each core exercise type is shown; author 3-5 lessons per
# section and 4-6 mixed exercises per lesson (see § sections/<sid>.toml).
id = "s29"
codename = "CHAPTER 29 // BEYOND THE AUTHORING FIRE"
short = "Beyond the Authoring Fire"
title = "Reproduce the Traveling Fault"
build = "complete the final unfamiliar tradeoff: diagnose a clean-package persistence fault, defend save-root, error, resource, and distribution choices, then ship dist/lanterns-of-briar-glen; cumulatively require the full spine and pass all tests/scenarios in source and the copied executable while preserving s26."
brief = "TODO: Phase 3 authors Beyond the Authoring Fire from the approved Arc."

[proof]
mode = "package"                   # run | build | guided | package (package only in final section)
expectedFiles = ["briar_glen/main.py", "requirements.txt", "dist/lanterns-of-briar-glen"]
runArgs = ["--arcanum-proof", "s29"]
expect = "TODO: exact deterministic milestone output"
requirementsFile = "requirements.txt"
artifactPath = "dist/lanterns-of-briar-glen"
packageArgs = ["lanterns-of-briar-glen.spec"]

# Media is NEVER authored by the tome AI. When this chapter needs a sprite, sound,
# music track, font, image, or video, add one or more [[assets]] sourcing guides:
# [[assets]]
# id = "player-sprite"
# kind = "sprite"
# lesson = "s29-l01"
# destination = "assets/player.png"
# sourceGuidance = "Explain how the learner chooses, downloads, renames, and places it."
# licenseGuidance = "Explain the license/attribution step the learner must perform."
# sources = [{ label = "Trusted asset library", url = "https://...", license = "..." }]
