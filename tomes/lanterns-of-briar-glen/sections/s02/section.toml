id = "s02"
codename = "CHAPTER II // VALUES BY LANTERNLIGHT"
short = "Values by Lanternlight"
title = "Values by Lanternlight"
build = "own language-values-expressions-data through literals, names, assignment, numeric and Boolean expressions, comparisons, and observable state in the learner-created game.py; retire the tool receipt only after the first saved source runs."
brief = '''<p>Rowan Vell has reached the first unlit marker in Briar Glen. Before a window, a map, or a creature can respond, the future game needs state that a reader can inspect. You will create the project’s first source file, name a few lantern facts, combine those facts into a small decision, and make Python report the result. The earlier text receipt remains evidence only until this saved source succeeds; then the source becomes the living trail marker and the receipt must retire.</p>'''

[proof]
mode = "guided"
mechanisms = ["terminal-print-working-directory", "terminal-change-directory", "terminal-check-python-version", "venv-create-environment", "venv-activate-environment", "venv-resolve-active-python", "editor-create-file", "editor-edit-file", "editor-save-file", "terminal-read-saved-file", "terminal-invoke-saved-file", "python-literal-values", "python-name-binding", "python-arithmetic-operators", "python-comparison-operators", "python-boolean-operators", "python-print-call", "terminal-delete-file"]
expectedFiles = ["game.py"]
guidedChecks = [
  "From the external project root with .venv active, read the saved learner-authored game.py and run python game.py; confirm it prints exactly 10 2 True from named health and oil state, derived arithmetic, comparisons, and Boolean logic.",
  "Only after that successful source run, use rm toolchain-check.txt to remove that exact temporary receipt, confirm the receipt is absent, and rerun python game.py to observe the same exact 10 2 True line without adding dependencies, Pygame, package files, or later-language features.",
]
supersedes = ["s01"]
protects = ["workspace-orientation", "isolated-python-environment", "external-file-action-loop", "tool-navigate", "tool-diagnose", "tool-install", "tool-create-open", "tool-edit-save", "tool-run-test", "language-environment-venv", "python-state-model", "state-expression-rules", "source-smoke-receipt", "language-values-expressions-data"]
