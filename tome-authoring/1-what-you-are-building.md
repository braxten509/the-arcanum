# 1. What you are building

A tome is one folder. The engine is 100% content-neutral: it renders whatever
the TOML provides, in any (human) language, teaching any (programming) language.

A tome can be laid out two ways; the engine loads both **identically** (via
`tome_layout.py`), so pick whichever is easier to edit. The **split layout** (below)
is recommended — small, single-purpose files are easier to find and change:

```
tomes/<id>/
  tome.toml          # CORE config only: [meta] [runtime] [content] [defaults]
                        #   [economy] [narrative] [progression]
  themes.toml          # [[themes]] palettes           (the big banks, split out of tome.toml)
  shop.toml            # [[shop]] items
  badges.toml          # [[badges]]
  intrusions.toml      # the HEX-DEFENSE bank — [[tiers]] (+ [[tiers.pool]])
  attacks_src.toml     # SPELL DUEL reference solutions you author (optional; in this tome's language)
  sections/
    <sid>/                 # one folder per chapter, ordered by [content].sections
      section.toml           # the chapter's own keys: id, codename, short, title, build, brief
      freestyle.toml         # [freestyle] — the Great Working (workbench) + its rubric/badge
      lessons/
        l01.toml             # one lesson per file ([[lessons]] + its readings/exercises),
        l02.toml             #   ordered by filename — just drop in l03.toml to add one
  generated/            # MACHINE-WRITTEN — never edit by hand. Regenerated by the tooling.
    attacks.toml           # the SPELL DUEL bank, built from attacks_src.toml by gen_attacks.py
  save/                 # runtime user progress — created by the engine. NEVER author
                        # this; exclude it when sharing/zipping a tome.
```

**Flat layout (still supported):** put every bank back inline in one big `tome.toml`
(with `[[themes]]`, `[[shop]]`, `[[badges]]`, `[[progression.intrusionTiers]]`), and
each chapter as a single flat `sections/<sid>.toml`. Mixing is fine per file: a split
bank sibling wins over the same table inline; a `sections/<sid>/` folder wins over a
flat `sections/<sid>.toml`. To convert a flat tome to the split layout losslessly:
`python3 tools/maintenance/split_tome.py tomes/<id>`.

**Hard rules:**
- The folder name is the tome **id**: letters, digits, `-`, `_` only.
- Everything is TOML 1.0, parsed by Python `tomllib`. It must parse — one syntax
  error and the tome fails to load.
- Dropping the folder in is the whole install; it appears in the OTHER TOMES
  picker automatically (`?tome=<id>` in the URL also selects it).
- Lesson/brief bodies are **HTML strings inside TOML** (the browser never parses TOML).
- **The deliverable is the `tomes/<id>/` folder and its TOML — nothing else.**
  If you write a helper/generator script to emit the TOML, that script is scratch:
  delete it when done, or run it in a temp dir. Do NOT leave `gen_*.py`, build
  scripts, notes, or any other loose files in the repo — the only new paths that
  should exist after you finish are inside `tomes/<id>/`. In particular, never
  add anything under `skins/` (see the themes-not-skins policy in §2), and never
  touch the engine's own folders: `web/`, `runtimes/` (the one TOML-driven engine
  — `generic.py` — never edit it), `sounds/` (the platform's recorded SFX), or
  `global-configs/` (cross-tome, platform-level tuning: `sigil.toml`,
  `particles.toml`, `audio.toml`, and the per-language configs under
  `runtimes/`). Adding a language is the one exception — a new
  `global-configs/runtimes/<name>.toml` per §5 — and even that is a platform
  change, not a per-tome file. (`tools/gen_attacks.py` is a language-neutral engine
  tool — don't edit it; it reads *your* per-tome `attacks_src.toml`, §4.)

**TOML craft you must use:**
- Multiline **literal** strings `'''…'''` for HTML bodies, code, and expected
  output — no escaping inside, exactly what you write is what ships.
- A leading newline right after `'''` is part of the string for `expect` values —
  the engine normalizes blank lines away for output comparison, but keep starter
  code tidy anyway.
- Inline arrays `["a", "b"]`; arrays-of-tables `[[lessons]]` for repeated items;
  sub-tables `[freestyle.badge]`.
- Single-quoted literal strings `'…'` for regexes (no backslash doubling).
